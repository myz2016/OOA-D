客户在筛选的时候，不希望提供价格与序号，只希望通过规格去查找符合条件的吉他。
由此可以将Guitar中的一些属性迁移至GuitarSpec类（吉他细则）：
移除：model、topWood、backWood、builder、type成员变量
依赖：GuitarSpec类

领悟：让类干它该干的事情。根据客户需求，发现移除的成员变量在Guitar类
中有些多余，它们都属于一些特性，所以就单独移除出去，形成一个新的类GuitarSpec。
那么Guitar中持有Guitar的引用就可以了。
如果Guitar中保留这些成员变量，并且新建一个类Guitar，其中也包含这些成员变量，
那么势必会造成代码的重复。所以这种改进方式也避免了代码的重复。
将成员变量移到一个新的类，这也是一种封装的体现。